{
	"name": "template-ca-html",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "live-server --port=3000 ",
		"test": "npx playwright install chromium && npx playwright test --project=chromium",
		"test:cli": "npx playwright install chromium &&  npx playwright test --reporter=json --project=chromium > playwright-report.json",
		"parse-results": "node parse-results.js",
		"test:auto": "npm run test:cli && npm run parse-results"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"live-server": "^1.2.2"
	},
	"devDependencies": {
		"@playwright/test": "^1.46.0",
		"@types/node": "^22.1.0"
	}
}
